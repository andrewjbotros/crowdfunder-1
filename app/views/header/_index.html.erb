<header class="site-menu">
  <a href="/" class="site-logo"><%= site_name %></a>

  <% if signed_in? %>
    <%= render 'header/user' %>
  <% else %>
    <%= render 'header/guest' %>
  <% end %>
</header>